import { CtIfDirective } from './ct-if.directive';

describe('CtIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CtIfDirective();
    expect(directive).toBeTruthy();
  });
});
